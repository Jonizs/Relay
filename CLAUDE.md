# Commit policy

Always commit changes instantly after making them — do not wait for the user to ask. Use clear, concise commit messages describing the why.

Always push to `main` immediately after committing — do not wait for the user to ask. Note: pushing to `main` triggers the GitHub Actions deploy to GitHub Pages.
